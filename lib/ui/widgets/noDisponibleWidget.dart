
import 'package:flutter/cupertino.dart';

class IndiponibleWidget extends StatelessWidget {
  const IndiponibleWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}